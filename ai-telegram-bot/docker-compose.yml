version: '3.8'

services:
  omnichannel-bot:
    build: .
    container_name: ai_bot_container
    ports:
      - "8000:8000"
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
    # Ensures the bot stays alive 24/7 if it crashes
